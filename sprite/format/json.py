# TODO: Get rid of XML format and add JSON format.
# I don't want to maintain the XML format anymore, get rid of it.
# But add another format that:
# * is easier for programmatic processing than the CSV-ish text format,
# * tests my file format handling more by having more than one format and
# * makes it easier to think about how to organize my objects.
